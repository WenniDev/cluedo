[package]
name = "gateway"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "gateway"
path = "src/main.rs"

[dependencies]
socket = { path = "../socket" }
tungstenite = "0.29.0"
gpui = "0.2.2"
async-channel = "2"
